# 변경 기록

버전은 커밋 제목 앞에 붙습니다 (`git log --oneline`).
`1.15.60` 이전 기록은 커밋 로그를 참고하세요.

---

## 1.18.4 — 게스트를 최고 레벨로 시작

LV.1 로 시작하면 말랑 슬라임 하나만 나오고 보스는 Lv.10 이라 열리지 않습니다.
게임을 보여주려고 만든 모드인데 정작 재미있는 부분을 못 보여줬습니다.
이제 몬스터 전종 · 보스 · 일일 미션이 열린 `LV.14 팝 마스터` 로 시작합니다.

## 1.18.3 — 제목과 첫 카드 사이 여백

`.topbar` 아래가 5px 뿐이라 카드와 붙어 보였습니다. 제목 묶음이 두 줄이고
`h1` 에 아래 여백이 없어 체감이 더 좁았습니다. 16px 으로 올렸습니다 (7개 화면 공유).

## 1.18.2 — 포트폴리오 썸네일을 저장소에서 제외

`public/images/portfolio` 의 PNG 3장(6.9MB)은 앱이 참조하지 않는데 저장소와
배포본을 무겁게 만들었습니다. 로컬 파일은 두고 추적만 멈추고 히스토리에서도 제거했습니다.
클론이 40MB → 27MB 가 됐습니다.

## 1.18.1 — 변경 기록 문서 추가

## 1.18 — 가입 없이 체험하는 게스트 모드

로그인 화면의 **"가입 없이 먼저 체험하기"** 로 계정 없이 사냥할 수 있습니다.

Supabase 익명 로그인은 쓰지 않았습니다. 방문자마다 실제 `auth.users` 행이 생겨
커뮤니티 글쓰기 권한이 그대로 붙고(스팸), 랭킹이 오염되고, 무료 플랜 MAU 를 잡아먹습니다.
게다가 가입 트리거가 이메일에서 닉네임을 뽑는데 익명 사용자는 이메일이 `NULL` 이라
그대로면 로그인 자체가 실패합니다.

대신 서버 계정 없이 이 기기에서만 도는 신원(`GUEST_ID`)을 씁니다. 진행도 · 미션 ·
보스 기록 · 설정 · 튜토리얼은 전부 `userId` 로 키를 만드는 localStorage 기반이라
게임은 그대로 돌아갑니다. 막은 것은 서버로 나가는 읽기 · 쓰기뿐입니다.

- 점수 · 프로필 · 몬스터 기록 저장과 순위 조회를 건너뜁니다
- 커뮤니티 글 · 댓글 · 좋아요는 막고 가입 안내를 띄웁니다
- 설정에는 이메일 대신 체험 안내와 가입 유도가 나옵니다

## 1.17.2 — 하단 바 그라데이션

떠 있는 하단 바의 아래 · 좌우 틈으로 콘텐츠가 비쳐 보이던 문제를 고쳤습니다.
바의 바닥 간격은 `max(10px, env(safe-area-inset-bottom))` 인데 그라데이션을 `-10px` 로
고정해 둬서, 홈 인디케이터가 있는 기기(safe-area 34px 쯤)에서는 24px 가 노출됐습니다.

`backdrop-filter` 는 요소 경계에서 뚝 끊겨(페더링 불가) 부드러운 그라데이션과 함께 쓰면
블러 경계가 선으로 남습니다. 걷어내고 중간 정지점 8개짜리 그라데이션만 씁니다.

## 1.17.1 — 첫 화면 배경 되돌림

배경을 화면 전체로 넓혔더니 넓은 모니터에서 `object-fit: cover` 가 그림을 과하게
확대 · 잘라내 몬스터가 거의 보이지 않았습니다. 모바일 기준 폭(460px)으로 되돌렸습니다.

## 1.17 — 폰트 자체 호스팅 + Pretendard unicode-range 분할

첫 화면 폰트 다운로드를 약 **2,185KB → 363KB (−83%)** 로 줄였습니다.

- **Black Han Sans 자체 호스팅** — CSS 첫 줄의 `@import` 는 파싱을 멈추고
  `googleapis`(CSS) → `gstatic`(폰트) 왕복을 연달아 만듭니다. 워드마크 전용이라
  라틴 서브셋(9KB)만 받아 뒀습니다. 외부 도메인 요청이 0건이 됐습니다.
- **Pretendard 2MB 를 조각으로 분할** — 글자를 빼는 서브셋이 아니라 범위를 나누는
  것이라 어떤 글자도 사라지지 않습니다.

분할 기준이 핵심입니다. `U+AC00-D7A3` 을 12등분하면 한글 음절은 코드포인트 순서와
사용 빈도가 무관해서 히어로 한 화면만으로 11덩이(1,878KB)를 건드립니다. 그래서 앱 소스에
실제로 등장하는 한글 700자를 `app` 조각(122KB) 하나로 모으고, `kr01~kr12` 는 닉네임 ·
게시글용 폴백으로 남겼습니다. `app` 을 맨 마지막에 선언해 겹치는 코드포인트에서 이깁니다.

재생성: `python scripts/split-fonts.py`

## 1.16.3 — 첫 화면 배경 전체 확장

(1.17.1 에서 되돌림)

## 1.16.2 — README · 저장소 정리 · 랭킹 이미지 WebP

- 랭킹 이미지 4개를 1254px PNG(4.1MB) → 384px 무손실 WebP(332KB)로 변환.
  화면에서는 최대 120px 로만 쓰이던 것들입니다.
- `supabase_setup.sql` 을 "한 번" 실행하면 된다는 README 문구를 고쳤습니다.
  스키마가 바뀌면 다시 실행해야 하고, 그러지 않아 `profiles.xp` 없이 운영된 적이 있습니다.
- `.gitattributes` 로 CRLF 경고 제거, 없던 `.env.example` 추가.

## 1.16.1 — UI 제보 수정

- 넓은 화면의 휴대폰 목업 처리(상하 20px 여백 · 테두리 · 라운드) 제거
- 확대 차단 (viewport + iOS `gesture` 이벤트 + `touch-action`)
- 사냥 화면 좌우 흔들림 방어 — `scrollbar-gutter: stable` ·
  `overflow-x: clip` · `overscroll-behavior: none` · `.battle-page { touch-action: pan-y }`
- 커뮤니티 글 · 댓글이 프로필 이미지 대신 닉네임 첫 글자를 그리던 버그 수정

## 1.16 — 계정 데이터 기기 간 동기화

다른 기기로 로그인하면 레벨 · 도감이 초기화돼 보이던 문제를 고쳤습니다.

원인은 두 겹이었습니다. `profiles.xp` · `discovered_monsters` 컬럼이 실제 DB에 없어서
진행도 저장이 계속 실패했고, 코드가 그 실패를 버리고 localStorage 값을 대신 보여줘서
쓰던 기기에서는 정상으로 보였습니다.

- 저장 · 읽기 실패를 화면에 드러냅니다
- 프로필 행이 없으면 자동 생성하고, 기기에만 있던 진행도를 로그인 시 서버로 올립니다
  (XP 는 큰 쪽, 도감은 합집합 — 어느 방향으로도 줄지 않습니다)
- 미션 · 보스 스트릭 · 설정 · 조작 안내를 `user_state` 테이블로 옮겨 계정을 따라가게 했습니다

병합 규칙은 종류마다 다릅니다 — 진행도는 "잃지 않는 쪽", 설정은 "마지막에 저장한 쪽"이
이깁니다. 자세한 내용은 [README](README.md#데이터베이스) 를 보세요.

## 1.15.61 ~ 1.15.64

임시 프리뷰 파일 제거 · 글꼴 변경 · 간격 변경 · 문구 변경
