# Supabase 프로젝트 설정 → Project Settings → API 에서 복사합니다.
# 이 파일을 .env 로 복사한 뒤 값을 채우세요:  cp .env.example .env
#
# anon key 는 브라우저에 그대로 노출되는 공개 키입니다. 데이터 보호는 이 키를 숨기는 것이 아니라
# supabase_setup.sql 의 RLS 정책이 담당합니다. service_role 키는 절대 여기에 넣지 마세요.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
